import { BtnListenerDirective } from './btn-listener.directive';

describe('BtnListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnListenerDirective();
    expect(directive).toBeTruthy();
  });
});
