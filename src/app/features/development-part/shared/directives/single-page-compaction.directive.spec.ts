import { SinglePageCompactionDirective } from './single-page-compaction.directive';

describe('SinglePageCompactionDirective', () => {
  it('should create an instance', () => {
    const directive = new SinglePageCompactionDirective();
    expect(directive).toBeTruthy();
  });
});
